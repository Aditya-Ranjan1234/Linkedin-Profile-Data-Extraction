1. Create and activate a Python 3.10-3.13 virtual environment:
   python -m venv venv
   venv\Scripts\activate  # On Windows
   source venv/bin/activate  # On Mac/Linux

2. Install dependencies:
   pip install -r requirements.txt

3. Set environment variables in the .env file (edit with your credentials and API keys).

4. Run the main script:
   python -m src.automating_linkedin_profile_data_extraction_and_analysis.main

# For other commands (train, replay, test), use:
# python -m src.automating_linkedin_profile_data_extraction_and_analysis.main <command> 